import React from 'react';

import SectionTitle from '../section-title/section-title.component';

interface OfficeConstructionProps {}

// eslint-disable-next-line max-len
const OfficeConstruction: React.FC<OfficeConstructionProps> = () => (
  <SectionTitle align="center">Office Construction</SectionTitle>
);

export default OfficeConstruction;
